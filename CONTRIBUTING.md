# Contributing

Use Python 3.11+, type public APIs, keep protocol code free of I/O, and add assertions for all behavior.
Run `ruff check .`, `ruff format --check .`, `mypy src`, and `pytest` before submitting.
